'use client';

/**
 * 빠른 시작 프리셋 선택기.
 * 이전에는 16개 CSS 테마를 선택하는 UI였으나, 테마 시스템 제거로 더 이상 노출하지 않음.
 * 레이아웃별 프리셋 관리는 각 위젯 설정 페이지에서 직접 수행.
 */
export function QuickStartPresets(_props: { user: string; hasCustomTheme: boolean }) {
  return null;
}
